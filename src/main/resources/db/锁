mysql
乐观锁
操作数据库时默认此次操作不会造成冲突，不加锁。更新后判断是否冲突
实现方式：加个版本号每次操作后加1，先查询版本号，后操作时判断版本号是否和刚刚查询的版本号一致

悲观锁
操作数据库认为一定会有冲突，持有排它锁，其它写数据库的操作会阻塞等待锁释放
实现方式 共享锁和排他锁

共享锁
对不同事务，对统一资源可以共享同一把锁 lock in share mode
排它锁
对不同事务，对统一资源只能有一把锁 for update

行锁