java内存区域：
    堆（1.8之后包含字符串常量池，之前在方法区） 存储的全部是对象(对象的成员变量也在这里),还有数组 线程共享
    方法区：储存虚拟机加载的类信息class和static变量、常量、（编译后的代码）
    （运行时常量池属于方法区）
    栈：栈帧 每个方法会创建一个栈帧，方法结束时释放
（存储局部变量表，操作栈，方法返回值。局部变量表放着基本数据类型，还有对象的引用。基础数据类型的对象）
    程序计数器:用于保存当前正常执行的程序的内存地址
    本地方法栈:为native方法服务


outOfMemory
内存溢出就是你要求分配的内存超出了系统能给你的，系统不能满足需求（线程太多，内存不够创建新线程）
内存泄漏：指程序中动态分配内存给一些临时对象，但是对象不会被GC所回收，它始终占用内存

栈溢出有两种
StackOverflowError 方法递归没终止条件
outofmemory     方法中线程启动过多